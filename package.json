{
	"name": "pi-thinking-hotkeys",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Pi extension for Codex-style thinking effort hotkeys",
	"main": "index.ts",
	"scripts": {
		"test": "vitest run",
		"typecheck": "tsc --noEmit",
		"check": "npm run typecheck && npm test",
		"check:keys": "node scripts/check-alt-symbols.mjs"
	},
	"devDependencies": {
		"@mariozechner/pi-coding-agent": "^0.72.1",
		"@mariozechner/pi-tui": "^0.72.1",
		"@types/node": "^24.0.0",
		"typescript": "^5.7.3",
		"vitest": "^3.2.4"
	}
}
